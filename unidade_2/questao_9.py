"""Crie uma variável chamada altura_str e atribua a ela o valor "1.75" (uma string). Em seguida, converta essa
variável para o tipo float e armazene o resultado em uma nova variável chamada altura_float. Por fim, use o
print para exibir a mensagem: "A altura convertida é: [altura_float]"""

altura_str = "1.75"
converter = float(altura_str)

print(converter)
